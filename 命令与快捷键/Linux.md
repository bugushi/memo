# Linux

### 文件
key | action
-- | --
`unzip test.zip -d tmp/` | 解压文件

### 网络
key | action
-- | --
`lsof -i :3000` | 查看3000端口占用
`kill -9 <PID>` | 杀端口
`export http_proxy=http://127.0.0.1:1080` | 设置终端代理
